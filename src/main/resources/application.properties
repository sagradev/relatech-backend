spring.application.name=backend

# Configuração do banco de dados
spring.datasource.url=jdbc:postgresql://localhost:5432/relatech
spring.datasource.username=postgres
spring.datasource.password=gugulimao

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Porta do servidor
server.port=${PORT:8080}

spring.jpa.open-in-view=false

jwt.secret=${JWT_SECRET:relatech2024chave-secreta-minimo-256bits-segura!!xpto}

spring.mvc.cors.allowed-origins=https://relatech.vercel.app,http://localhost:5173
